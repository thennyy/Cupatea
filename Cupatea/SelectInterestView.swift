//
//  InterestView.swift
//  Cupatea
//
//  Created by Thenny Chhorn on 10/31/21.
//

import UIKit

class SelectInterestView: UIView {
    
    /*
    let titleLabel = UILabel(textColor: .accentColor, text: "Interested \nWith?", fontSize: 30, weight: .bold, alignment: .left)
    
    let selectWomanView: GenderView = {
        let view = GenderView()
        view.label.text = "Woman"
        return view
    }()
    let selectManView: GenderView = {
        let view = GenderView()
        view.label.text = "Man"
        return view
    }()
    let selectOtherView: GenderView = {
        let view = GenderView()
        view.label.text = "Everyone"
        return view
    }()
    lazy var stackView: UIStackView = {
       
        let stackView = UIStackView(arrangedSubviews: [selectWomanView, selectManView, selectOtherView])
        stackView.axis = .vertical
        stackView.distribution = .fillEqually
        stackView.spacing = 21
        return stackView
    }()
    
    override init(frame: CGRect) {
        super.init(frame: frame)
       
        addSubview(titleLabel)
        titleLabel.anchor(top: topAnchor, left: leftAnchor,right: rightAnchor, paddingTop: 30, paddingLeft: 45, paddingRight: 45)
        addSubview(stackView)
        stackView.anchor(top: titleLabel.bottomAnchor,
                         left: leftAnchor,
                         right: rightAnchor,
                         paddingTop: 30,
                         paddingLeft: 45,
                         paddingBottom: 0,
                         paddingRight: 51,
                         height: 200)
        
      //  stackView.centerXAnchor.constraint(equalTo: centerXAnchor).isActive = true
    }
    
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    */ 
}
